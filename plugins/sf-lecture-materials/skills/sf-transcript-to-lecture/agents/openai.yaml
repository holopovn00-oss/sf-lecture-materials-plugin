interface:
  display_name: "SF: транскрипт → учебный текст JSON"
  short_description: "Полное объяснение в JSON с абзацами и LaTeX"
  default_prompt: "Используй $sf-transcript-to-lecture для подготовки полного учебно-методического текста только в JSON со смысловыми абзацами, LaTeX и исходными привязками."
policy:
  allow_implicit_invocation: false
